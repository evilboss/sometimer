const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import SendInvitationModal from '../send_invitation_modal';

describe('invitations.components.send_invitation_modal', () => {
  it('should do something');
});
