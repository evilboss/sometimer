const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import CreateTeamButton from '../landing/create_team_button';

describe('core.components.create_team_button', () => {
  it('should do something');
});
