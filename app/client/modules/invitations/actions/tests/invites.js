const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../invites';

describe('invitations.actions.invites', () => {
  it('should do something');
});
