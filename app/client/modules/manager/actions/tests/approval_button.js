const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../timeLogApproval';

describe('manager.actions.approval_button', () => {
  it('should do something');
});
