const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../upload_file';

describe('team.actions.upload_file', () => {
  it('should do something');
});
