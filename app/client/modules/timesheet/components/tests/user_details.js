const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import UserDetails from '../user_details';

describe('timesheet.components.user_details', () => {
  it('should do something', (done)=> {
    done();
  });
});
