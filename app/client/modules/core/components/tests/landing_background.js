const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import LandingBackground from '../landing/landing_background';

describe('core.components.landing_background', () => {
  it('should do something');
});
