const {describe, it} = global;
import {expect} from 'chai';
import {shallow} from 'enzyme';
import AllowedToAddProjects from '../allowed_to_add_projects';

describe('users.components.allowed_to_add_projects', () => {
  it('should do something');
});
