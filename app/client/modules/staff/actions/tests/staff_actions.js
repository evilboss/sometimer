const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../staff_actions';

describe('staff.actions.staff_actions', () => {
  it('should do something');
});
