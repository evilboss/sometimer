const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../sub_projects';

describe('sub_projects.actions.sub_projects', () => {
  it('should do something');
});
