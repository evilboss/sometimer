# Meteor packages used by this project, one per line.
# Check this file (and the other files in this directory) into your repository.
#
# 'meteor add' and 'meteor remove' will edit this file for you,
# but you can also edit it by hand.

meteor-base                     # Packages every Meteor app needs to have
mobile-experience               # Packages for a great mobile UX
mongo                           # The database Meteor supports right now
blaze-html-templates            # Compile .html files into Meteor Blaze views
reactive-var                    # Reactive variable for tracker
jquery                          # Helpful client-side library
tracker                         # Meteor's client-side reactive programming library

standard-minifier-js            # JS minifier run for production mode
es5-shim                        # ECMAScript 5 compatibility for older browsers.
ecmascript                      # Enable ECMAScript2015+ syntax in app code
# Meteor Standard Packages
check                           # Check whether a value matches a pattern
random                          # Random number generator and utilities
accounts-password               # Password support for accounts
email                           # Send email messages
reactive-dict                   # general-purpose reactive datatype for use with tracker.

# Atmosphere Third Party packages
aldeed:simple-schema            # A simple schema validation object with reactivity. Used by collection2 and autoform.
aldeed:collection2              # Automatic validation of insert and update operations on the client and server.
aldeed:autoform                 # UI components and helpers to easily create basic forms with automatic insert and update events
matb33:collection-hooks         # Extends Mongo.Collection with before/after hooks for insert, update, remove, find, and findOne.
gildaspk:autoform-materialize   # Adds materialize templates for autoform.
alanning:roles                  # Authorization package for Meteor
fortawesome:fontawesome         # Font Awesome (official): 500+ scalable vector icons, customizable via CSS, Retina friendly
mdg:validation-error            # A simple wrapper for Meteor.methods
kadira:flow-router              # Same as kadira:flow-router, but with SSR support
kadira:dochead                  # DocHead is an isomorphic way to manipulate document.head for Meteor apps.
materialize:materialize@=0.97.7         # Materialize (official): A modern responsive front-end framework based on Material Design
fourseven:scss                  # Style with attitude. Sass and SCSS support for Meteor.js (with autoprefixer and sourcemaps)
meteorhacks:picker              # Server Side Router for Meteor
percolate:migrations            # A simple migration system for Meteor supporting up/downwards migrations and command line usage. Forked to experiment with package data migrations.
thereactivestack:blazetoreact   #Transform a Blaze template into a React.js component while keeping the reactivity
juliancwirko:postcss            # Use PostCSS with Meteor
kevohagan:sweetalert            #  a responsive, themable, cross-browser replacement for JavaScript's alert() with beautiful dialog prompts.
ultimatejs:tracker-react        #No-Config reactive React Components with Meteor. Apply as composition, mixin or decorator.
lookback:emails                 # Send HTML emails with server side Blaze templates. Preview and debug in the browser.
browser-policy                  # Configure security policies enforced by the browser
edgee:slingshot                 # Directly post files to cloud storage services, such as AWS-S3.
themeteorchef:bert              # A client side, multi-style alerts system for Meteor.
lfergon:exportcsv               # Meteor export mongo collection to csv
#custom package section
#dev only packages
meteortoys:allthings # Insanely Handy Develo1pment Tools

