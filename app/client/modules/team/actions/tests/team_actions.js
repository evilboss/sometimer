const {describe, it} = global;
import {expect} from 'chai';
import {spy, stub} from 'sinon';
import actions from '../team_actions';

describe('team.actions.team_actions', () => {
  it('should do something');
});
